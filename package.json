{
  "name": "delilog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "vercel --prod",
    "preview": "vercel dev"
  },
  "devDependencies": {
    "vercel": "latest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  }
}